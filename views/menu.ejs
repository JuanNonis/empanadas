<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Menú - Casa de Empanadas</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="/styles.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Luckiest+Guy&family=Pacifico&display=swap" rel="stylesheet">
  <style>
    .menu-img {
      max-width: 80px;
      max-height: 80px;
      object-fit: contain;
      margin-bottom: 8px;
      display: block;
      margin-left: auto;
      margin-right: auto;
    }
  </style>
</head>
<body>
  <nav class="navbar navbar-expand-lg mb-4">
    <div class="container">
      <a class="navbar-brand d-flex align-items-center gap-2" href="/">
        <img src="/pinguino.png" alt="Pingüino" style="width:40px; height:auto;">
        Casa de Empanadas
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="#navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item"><a class="nav-link" href="/menu">Menú</a></li>
        </ul>
      </div>
    </div>
  </nav>
  <div class="container mt-5">
    <h1 class="mb-4">Menú de Empanadas y Pizzas</h1>
    <div class="row">
      <% if (menu && menu.length) { %>
        <% menu.forEach(item => { %>
          <div class="col-md-4 mb-3">
            <div class="card h-100">
              <div class="card-body">
                <h5 class="card-title"><%= item.nombre %></h5>
                <p class="card-text">Precio: $<%= item.precio %></p>
                <p class="card-text"><%= item.descripcion %></p>
              </div>
            </div>
          </div>
        <% }) %>
      <% } else { %>
        <p>No hay productos en el menú.</p>
      <% } %>
    </div>
    <a href="/" class="btn btn-secondary mt-4">Volver a la página principal</a>
  </div>
</body>
</html> 